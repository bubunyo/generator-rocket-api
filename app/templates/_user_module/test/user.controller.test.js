import { nuke } from '../../../helpers/test_helpers';
import User from '../user.controller';
import server from '../../../server';

describe('User::Controller', async () => {
  it('user controller test', async () => {
    expect(1 + 1).toBe(2);
  });
});
